# Project-Scoped Rules for AI Document Agent

## Deployment Requirements
- Always terminate any existing or old running python/streamlit processes before launching or deploying new instances.
- Ensure that the SQLite file database (`chroma_db`) is not locked by other active processes.
